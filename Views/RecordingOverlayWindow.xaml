<Window x:Class="whisperMeOff.Views.RecordingOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowState="Normal"
        ResizeMode="NoResize"
        Width="216" Height="60">

    <Border Background="#B3000000" CornerRadius="30">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Blinking Red Dot -->
            <Ellipse Grid.Column="0" Width="14" Height="14" Fill="#EF4444" Margin="20,0,10,0"
                     VerticalAlignment="Center">
                <Ellipse.Effect>
                    <DropShadowEffect Color="#EF4444" BlurRadius="10" ShadowDepth="0"/>
                </Ellipse.Effect>
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                               From="1" To="0.3" Duration="0:0:0.5"
                                               AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>

            <!-- Recording Text -->
            <TextBlock Grid.Column="1" Text="Recording" FontSize="17" FontWeight="SemiBold"
                       Foreground="White" VerticalAlignment="Center" Margin="0,0,25,0"/>
        </Grid>
    </Border>
</Window>
